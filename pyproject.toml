[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hacathon-open-ai"
version = "0.1.0"
description = "Women Safety AI Moderator"
dependencies = [
    "gradio",
    "fastapi",
    "uvicorn",
    "requests"
]